# HubLink
This PowerShell script automates the creation of symbolic links for files from selected directories into a central folder. It allows users to select all, specific, or exclude certain directories. It validates inputs, handles errors, and ensures efficient file organization without duplication.
